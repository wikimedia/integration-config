- job:
    parent: null
    name: base
    abstract: true
    description: |-
      The recommended base job.

      All jobs ultimately inherit from this.  It runs a pre-playbook
      which copies all of the job's prepared git repos on to all of
      the nodes in the nodeset.

      It also sets a default timeout value (which may be overidden).

    secrets:
      - name: wmcs_cloud_config
        secret: wmcs_cloud_config

    pre-run: playbooks/base/pre.yaml
    post-run:
      - playbooks/base/post-logs.yaml
    roles:
      - zuul: integration/zuul-jobs
    timeout: 1800

- job:
    parent: base
    name: echo
    description: |-
      Echo a message.

      Used for very basic testing of pipeline and project configurations.
    nodeset: trixie
    run: playbooks/echo/run.yaml
    vars:
      message: "hello world"

- job:
    parent: base
    name: namespace-test
    description: |-
      List the contents of the working directory in the local executor
      context.

      Used for very basic testing of namespace nodsets.
    nodeset: namespace
    run: playbooks/run-on-executor/run.yaml
    vars:
      command: "ls -lR ."
